import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const HomeBanner = () => {
  return (
    <View styles={styles.container}>
      <Text>This is a HomeBanner</Text>
    </View>
  )
}

export default HomeBanner

const styles = StyleSheet.create({})
